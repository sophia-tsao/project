info:
  name: Generate a problem
  type: http
  seq: 6

http:
  method: GET
  url: "{{host}}/problem/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
