info:
  name: Get current user
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/auth/me/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
