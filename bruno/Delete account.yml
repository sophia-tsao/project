info:
  name: Delete account
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{host}}/auth/delete/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
